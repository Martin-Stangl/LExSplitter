<!DOCTYPE html>
<html>
    <head>
        <title>{% block title %}LExSplitter{% endblock %}</title>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
        {% block stylesheets %}
            {# Bootstrap --> #}
            <link href="{{ asset('css/bootstrap.css') }}" rel="stylesheet" media="screen">
            {# Bootstrap Combobox #}
            {% stylesheets '@TheBlackLionLExSplitterBundle/Resources/public/css/*' filter='cssrewrite' %}
                <link href="{{ asset_url }}" rel="stylesheet" media="screen">
            {% endstylesheets %}
            {# HTML5 Shim and Respond.js add IE8 support of HTML5 elements and media queries #}
            {% include 'BraincraftedBootstrapBundle::ie8-support.html.twig' %}
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <link rel="apple-touch-icon" type="image/png" href="{{ asset('apple-touch-icon.png') }}" />
        <link rel="apple-touch-startup-image" type="image/png" href="{{ asset('apple-startup-image.png') }}" />
    </head>
    <body>
        <div class="container">
            {% block body %}{% endblock %}
        </div>
        {% block javascripts %}
            {# jQuery (necessary for Bootstraps JavaScript plugins) #}
            <script src="{{ asset('js/jquery.js') }}"></script>
            {# Include all JavaScripts, compiled by Assetic #}
            <script src="{{ asset('js/bootstrap.js') }}"></script>
            {# Bootstrap Combobox #}
            {% javascripts '@TheBlackLionLExSplitterBundle/Resources/public/js/*' %}
                <script src="{{ asset_url }}"></script>
            {% endjavascripts %}
        {% endblock %}
    </body>
</html>
